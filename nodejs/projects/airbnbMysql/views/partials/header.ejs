<header class="bg-blue-600 text-white p-4">
  <div class="container mx-auto flex justify-between items-center">
      <!-- Logo -->
      <div class="text-2xl font-bold">
          Your Logos
      </div>
      <!-- Navigation -->
      <nav class="space-x-4">
        <a href="/" class="<%= currentPage === 'index'?'bg-red':'underline' %> hover:underline">airbnb</a>
        <a href="/homes" class="<%= currentPage === 'home'?'bg-red':'underline' %> hover:underline">Homes-List</a>
        <a href="/favourites" class="<%= currentPage === 'favourites'?'bg-red':'underline' %> hover:underline">Favourites List</a>
        <a href="/bookings" class="<%= currentPage === 'bookings'?'bg-red':'underline' %> hover:underline">Bookings</a>
        <a href="/host/host-home-list" class="<%= currentPage === 'Host-homes'?'bg-red-400':'hover:bg-red-400' %> py-2 px-4 rounded transition duration-300">Host Homes</a>
          <a href="/host/add-home" class="hover:underline">Add Home</a>
      </nav>
      <!-- Dropdown -->
      <div class="relative">
          <button class="focus:outline-none focus:shadow-outline">
              Account
          </button>
          <!-- <div class="absolute right-0 mt-2 w-48 bg-white text-black rounded-md shadow-lg py-2">
              <a href="/login" class="block px-4 py-2 hover:bg-gray-200">Login</a>
              <a href="/signup" class="block px-4 py-2 hover:bg-gray-200">Signup</a>
          </div> -->
      </div>
  </div>
</header>